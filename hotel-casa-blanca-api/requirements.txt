# ---- Framework web ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30

# ---- Base de datos (async) ----
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29

# ---- Validacion / configuracion ----
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1        # habilita EmailStr en Pydantic

# ---- Seguridad ----
pyjwt[crypto]>=2.8          # access_token + refresh_token
argon2-cffi>=23.1          # hash de contrasenas (argon2id)

# ---- Integraciones externas ----
google-auth>=2.30          # verificar id_token de Google (POST /auth/google)
httpx>=0.27                # llamadas async a la API de Mercado Pago

# ---- Vistas ----
jinja2>=3.1                # pagina minima de retorno (/pagos/retorno)

# ---- Desarrollo ----
pytest>=8.2                # tests de humo (httpx ya sirve de cliente ASGI)
pytest-asyncio>=0.23       # asyncio_mode=auto
ruff>=0.5                  # linter/formateador
